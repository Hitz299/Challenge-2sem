import { StyledBtnLocalizarContainer } from "../../style/styled-funcionalidades";
import Button from "../Button/Button";

export default function RodapeLocalizarVeiculos() {
  return (
    <>
    
        <StyledBtnLocalizarContainer>

            <Button titulo="Localizar Veículo" click={() => {}}/>
            <Button titulo="Localizar Oficina" click={() => {}}/>

        </StyledBtnLocalizarContainer>
    
    
    </>
  )
}
